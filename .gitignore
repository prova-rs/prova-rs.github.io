# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Browser-automation scratch (Playwright MCP writes screenshots and console
# logs here; jj auto-snapshots new files, so leaving it untracked is not enough)
/.playwright-mcp

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
